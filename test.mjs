import test from 'node:test';import assert from 'node:assert/strict';import {quote,plans} from './pricing.mjs';
test('exact provincial rates',()=>{assert.deepEqual(plans,{BC:[[500,65],[1000,75],[2000,85]],AB:[[500,65],[1000,75],[2000,85]],MB:[[100,70],[500,85],[2000,95]],SK:[[1000,55],[2000,65]],ON:[[1000,55],[2000,65]]});});
test('every plan, TV, phone, and auto-pay combination',()=>{for(const [province,options]of Object.entries(plans))for(const [speed,base]of options)for(const [tv,price]of Object.entries({none:0,essential:35,plus:55,ultimate:95}))for(const phone of [true,false])for(const autopay of [true,false]){const promo=['SK','ON'].includes(province);const discount=!promo&&autopay?5:0;const p=quote(province,speed,tv,phone,autopay);assert.equal(p.total,base-discount+price+(phone?25:0));assert.equal(p.promo,promo);assert.equal(p.dueToday,0);}});
test('invalid plan and add-on rejected',()=>{assert.throws(()=>quote('QC',1000));assert.throws(()=>quote('BC',100));assert.throws(()=>quote('ON',1000,'fake'));});
